NEXT STEPS — Planning doc
=========================
Snapshot of open work in docs/future-ideas/ (excluding completed/).
User to fill in the BUGS / ISSUES section below before we start work.


===========================================================
BUGS / ISSUES TO FIX FIRST  (user fills this in)
===========================================================

-
-
-
-
-


===========================================================
MODULAR REBUILD TRACK (the planned next big push)
===========================================================

Foundational / must land first
------------------------------
- phase56-component-modularization.md
    Large, 17 sub-phases. In-tree folder-shape refactor
    (checks/agents/advisors/gates → role-based filenames,
    Next.js-style: check.py + contract.yaml + config.yaml).
    Merges phase-17, phase-43, and module-system shape.
    This is the gateway doc.

- module-system-design.md
    Large. Partially superseded by phase56.
    External-plugin loader, contracts, licensing, lazy imports.
    Comes AFTER phase56 lands.

- concurrent-scans-overhaul.md  +  concurrent-scans-design.md
    Large, 7 phases / 15+ files / 58+ state refs.
    State singleton → per-scan ScanContext.
    PREREQUISITE for the module system (per concurrent-scans-overhaul §8.4
    and project memory). Confirm completion state before starting phase56.

Module deliverables (after foundation)
--------------------------------------
- phase55-terminal-dashboard-stream-client.md
    Small. Hard prerequisite of module-phase-1 (SSE client lib).

- module-phase-1-terminal-dashboard.md
    Medium. Full-screen Textual dashboard CLI.

- module-phase-2-scan-reporter.md
    Medium. Markdown notebook generator for completed scans.

- module-phase-3-scope-wizard.md
    Medium. Interactive scope-builder CLI wizard.


===========================================================
SMALL / BUG-FIX-ADJACENT CANDIDATES  (warm-up work)
===========================================================

- phase46-fakobanko-semantic-cache.md
    Small. Add CAG service container to fakobanko scenario.
    Test-infra fix.

- guardian-interactive-approval.md
    Small. Interactive scope-violation approval flow.
    Optional feature.

- performance-tuning.md
    Small. Advisory doc only — query tuning, batch sizing,
    concurrency guidance. No implementation required.

- schema-migration-tooling.md
    Deferred. Standalone migration script — revisit when
    persistent DBs land and schema stabilizes.


===========================================================
LARGER FEATURE / ARCHITECTURE WORK  (NOT on the modular path)
===========================================================

- phase32-nist-cui-hardening.md
    Large. CUI compliance: encryption, auth, audit, retention.
    Six sub-phases (32a–32f). Critical for regulated use.

- phase47-raw-check-io-capture.md
    Medium. Persist raw HTTP request/response from checks
    (schema, wrapper, API endpoint, UI drill-down, redaction).

- phase38-non-deterministic-sampling.md
    Medium. Multi-iteration checks for probabilistic findings.
    Reduces false-negative risk on AI checks.

- compliance-framework-selector.md
    Medium. Framework-filtered compliance reports.
    Status: "Ready to implement (co-implement with
    compliance-framework-badges.md)".

- phase54-swarm-enhancements.md
    Medium. Umbrella of 5 deferred swarm improvements
    (cross-process events, OPSEC, telemetry, recovery).

- concurrent-scans-swarm.md
    Medium. Make swarm coordinator session-scoped.
    Depends on Phase B of concurrent-scans-overhaul.

- phase48-oversight.md
    Large. API spec generation, architecture docs, contract
    templates, E2E tests. "Eliminate dark code."

- phase49-observability.md
    Large. Structured logging, metrics, scan telemetry,
    health endpoints. Five workstreams.


===========================================================
AMBIGUOUS / NEEDS DECISION
===========================================================

- future-improvements.md
    Unscoped backlog: prompt router tuning, team chat,
    DB resilience, proof advisors, offline modes, etc.


===========================================================
KEY DEPENDENCIES
===========================================================

1. concurrent-scans (both design docs) must land BEFORE module
   system is widely adopted.
2. phase56 (in-tree restructure) is the foundation for
   module-system (external plugins root).
3. phase55 (SSE client) is a HARD prerequisite of module-phase-1.
4. phase32-nist-cui-hardening is a parallel track — independent
   of modular rebuild but compliance-critical.


===========================================================
OPEN QUESTIONS BEFORE STARTING
===========================================================

- Where does concurrent-scans actually stand right now?
  (Memory says mid-refactor; need to verify before phase56.)
- After bug-fix list is filled in, decide: warm up with phase46
  (small) before tackling phase56, or jump straight in?
