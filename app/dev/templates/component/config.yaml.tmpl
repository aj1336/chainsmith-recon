# Tunables only (Phase 56 §5). Identity lives in contract.yaml.

enabled: true
on_critical: annotate

defaults:
  timeout_seconds: 30
  requests_per_second: 10
  retry_count: 1
  delay_between_targets: 0.1
