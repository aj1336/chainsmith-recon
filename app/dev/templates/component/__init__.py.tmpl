"""Re-export the entry class so `from {package} import {class_name}` resolves to
the same class object the loader instantiates (identity-preserving, §3.1)."""

from {package}.{entry_stem} import {class_name}

__all__ = ["{class_name}"]
